%table.table
  %thead
    %tr
      %th Дата
      %th Плательщик/Получатель
      %th Примечание
      %th Приход
      %th Расход

  %tbody
    - campaign.payments.each do |payment|
      %tr
        %td= payment.time.to_date
        %td= payment.contributor
        %td= payment.notes
        %td= payment.amount if payment.earning?
        %td= payment.amount if payment.expense?
    %tr
      %td{colspan: 3} ИТОГО:
      %td= campaign.payments.map {|i| i.earning? ? i.amount : 0}.inject(0, :+)
      %td= campaign.payments.map {|i| i.expense? ? i.amount : 0}.inject(0, :+)


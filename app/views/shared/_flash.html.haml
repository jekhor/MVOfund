.row
  .col-md-12
    - flash.each do |type, message|
      .alert.fade.in{class: bootstrap_class_for(type), role: 'alert'}
        %button.close{'data-dismiss': 'alert'} ×
        %span.sr-only= type.capitalize
        = message
